[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
cache_path = "cache"

# All exploit tests fork mainnet — set MAINNET_RPC_URL in your .env
# fork_block_number can be overridden per test via --fork-block-number

[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
